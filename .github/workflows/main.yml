on: 
  push:
  workflow_dispatch:


jobs:
  hello_world_job:
    runs-on: ubuntu-latest
    name: A job to say hello
    steps:
      - name: Hello world action step
        id: hello
        uses: dhanesh012/hello-world-javascript-action@v1.2
        with:
          who-to-greet: 'dhan'
      # Use the output from the `hello` step
      - name: Get the output time
        run: echo "The time was ${{ steps.hello.outputs.time }}"
      - name: Get the output time
        run: |
          echo "Hi ${{ secrets.abc }}"
          echo "Hi ${{ secrets.ABC }}"
          echo "Hi ${{ secrets.cde }}"
